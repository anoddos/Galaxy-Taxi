@page "/subscription/payment"
@using GalaxyTaxi.Shared.Api.Interfaces
@using GalaxyTaxi.Shared.Api.Models.Payment
@inject ISubscriptionService SubscriptionService
@inject NavigationManager NavigationManager
@inject IPaymentService PaymentClient

@page "/payment"


<MudForm>
    <MudTextField Label="Amount" @bind-Value="paymentAmount" />
    <MudButton @onclick="ProcessPayment">Process Payment</MudButton>
</MudForm>

@code {
    private decimal paymentAmount;

    private async Task ProcessPayment()
    {
        var paymentRequest = new PaymentRequest()
        {
            Amount = (int)(paymentAmount * 100) // Convert to cents
        };

        var paymentResponse = await PaymentClient.ProcessPayment(paymentRequest);

    // Payment failed, display an error message or perform other actions
    }
}}